#pragma once
/* Optional compile-time fallbacks. Prefer captive portal / NVS. */
/* secrets.h is gitignored — never commit real credentials. */
#define WIFI_SSID_DEFAULT        ""
#define WIFI_PASSWORD_DEFAULT    ""
#define MQTT_HOST_DEFAULT        ""
#define MQTT_PORT_DEFAULT        1883
#define MQTT_USER_DEFAULT        ""
#define MQTT_PASSWORD_DEFAULT    ""
#define MQTT_TOPIC_BASE_DEFAULT  "home"
#define HUB_HOSTNAME_DEFAULT     "thread-hub"
